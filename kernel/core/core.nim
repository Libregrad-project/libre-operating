# TODO: We need to make the kernel system, which can include / import external Assembly (ASM) code, 
# Which then will be used inside the code-base.

# To import Assembly in Nim: {.importasm: "boot/boot.asm";}.